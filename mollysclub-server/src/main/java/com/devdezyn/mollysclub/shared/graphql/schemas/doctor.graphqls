type Doctor {
  id: ID!
  specialization: String
  mobile: String
  gender: String!
  age: Int
}

# Root
extend type Query {
  findAllDoctors: [Doctor]!
  countDoctors: Long!
}

# Root
extend type Mutation {
  createDoctor(title: String!, description: String!, patientId: Int): Doctor!
}
