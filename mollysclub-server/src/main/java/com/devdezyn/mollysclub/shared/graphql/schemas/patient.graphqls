type Patient {
  id: ID!
  gender: String!
  birthday: String!
  gender: String!
}

# Root
extend type Query {
  findAllPatients: [Patient]!
  countPatients: Long!
}

# Root
extend type Mutation {
  createPatient(name: String!, age: Int): Patient!
}
